// ─── Components Directory ────────────────────────────────────────
// Reusable React components will be added here in Phase 2+.
//
// Planned structure:
// - components/ui/       → Generic UI elements (Button, Input, Card)
// - components/forms/    → Form components (LoginForm, RegisterForm)
// - components/layout/   → Layout components (Navbar, Sidebar)
// - components/vault/    → Vault-specific components (DocumentCard, ShareModal)

export {};
